source("renv/activate.R")
options(renv.settings = list(use.cache = ifelse(.Platform$pkgType == "source", FALSE, TRUE)),
        repos = c(CRAN = "https://cran.microsoft.com/snapshot/2020-07-14/"),
        scipen = 10)
